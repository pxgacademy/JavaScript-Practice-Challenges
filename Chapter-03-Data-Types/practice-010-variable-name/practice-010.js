//

const favoriteFood = "Burger";

const city = "Chattogram";
const City = "Dhaka";
console.log(city, City);

const userName = "Johan";
const username = "Jovan";
console.log(userName, username);

const myHomeAddress = 'Hathazari, Chattogram'

const super = 24; // super is a keyword. so the variable will not work.
const class = 18; // class is also a keyword.
const 2ndPosition = 'Sakib'; // first letter should not be numeric